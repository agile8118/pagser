{
  "name": "pagser",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:agile8118/pagser.git",
  "author": "Joseph Heidari <agile.8272@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "cd packages/common && yarn build && cd ../reusable && yarn build && cd ../web && yarn build && cd ../styles && yarn build && cd ../server && yarn build",
    "setup": "NODE_ENV=development yarn install && yarn build && cd packages/server && yarn seed",
    "start": "cd packages/server && yarn prod && echo 'Server started with PM2. Run `pm2 logs` to view logs.'",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "playwright": "playwright",
    "seed:master": "yarn workspace @pagser/stressor seed",
    "seed:e2e": "yarn workspace @pagser/stressor seed:e2e",
    "seed:db": "yarn workspace @pagser/stressor seed:db"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
